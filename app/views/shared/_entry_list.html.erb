<% @entries.each do |entry| %>
<div class="entry">
	<h2><%=h entry.title %></h2>
	<%= entry.content %>
	<p class="entry-info">This entry was posted on <%=h entry.published_on %>.</p>
</div>    
<% end %>